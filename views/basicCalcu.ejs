<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
  integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<!-- Use to helps keep the footer at the bottom even if there's little content-->
<body class="d-flex flex-column min-vh-100">
<!-- Navigation link center-->
  <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #3a3f44;">
    <div class="container">
      <a class="navbar-brand" href="/basicMath">Basic Math</a>
      <ul class="nav justify-content-end">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/about" style="color: white;">About us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/demo" style="color: white;">Description</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/userinfo" style="color: white;">User Information</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/basicCalcu" style="color: white;">Basic Calculator</a>
        </li>
      </ul>
    </div>
  </nav>
   
  <!-- Backgground and header -->
  <div class="container flex-grow-1">
    <h2>Basic Calculator</h2>
    <img class="bg-img" src="/images/background.jpg" alt="Bg images">

    <!-- Basic Calculator -->
    <div class="mt-1">
      <div class="card bg-white text-dark">
        <div class="card-body">
          <form action="/basicCalcu" method="POST">
            <div class="mb-1">
              <label for="num1" class="form-label">Enter first number</label>
              <input type="number" name="num1" class="form-control" required>
            </div>
            <div class="mb-2">
              <label for="num2" class="form-label">Enter second number</label>
              <input type="number" name="num2" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary">Calculate sum</button>
          </form>
          
          <!-- Funtion result -->
          <% if (sum !== null) { %>
            <h5 class="mt-2 text-center">The result of <%= num1 %> + <%= num2 %> is <%= sum %>.</h5>
            <h5 class="mt-2 text-center">The result of <%= num1 %> * <%= num2 %> is <%= product %>.</h5>
            <h5 class="mt-2 text-center">The result of <%= num1 %> / <%= num2 %> is <%= quotient %>.</h5>
            <h5 class="mt-2 text-center">The result of <%= num1 %> - <%= num2 %> is <%= different %>.</h5>
            <h5 class="mt-2 text-center">The result of <%= num1 %> + <%= num2 %> / 2 is <%= average %>.</h5>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-white text-dark text-center py-3">
    <blockquote class="blockquote mb-0">
      <p>"The only limit to our realization of tomorrow is our doubts of today."</p>
      <footer class="blockquote-footer">Franklin D. Roosevelt</footer>
    </blockquote>
  </footer>
</body>
</html>
